export function PageFooter() {
  return (
    <footer className="mt-auto pt-8 text-center text-xs text-muted sm:text-sm">
      <p>Data is fetched server-side with provider fallbacks and one-hour caching.</p>
    </footer>
  );
}
